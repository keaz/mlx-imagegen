[project]
name = "mlx-imagegen"
version = "0.1.0"
description = "Interactive MLX image generator (FLUX via mflux) with selectable models and style modes"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "mflux",
    "mlx-whisper>=0.4.3",
    "sounddevice>=0.5.5",
]

[tool.uv]
package = false
